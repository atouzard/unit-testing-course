<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Adventurer guild{% endblock %}</title>
        <link rel="stylesheet" href="{{ asset('styles/styles.css') }}">
        <!-- CSS only -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
        <script src="https://kit.fontawesome.com/fab65cceab.js" crossorigin="anonymous"></script>
    </head>
    <body>

    <header>
        <nav class="navbar navbar-expand-md navbar-dark bg-dark px-4">
            <a class="navbar-brand" href="#">Guild</a>
            <div class="collapse navbar-collapse" id="navbarCollapse">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('main_controller') }}">Home</a>
                    </li>
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('admin_list') }}">Admin panel</a>
                    </li>
                    <li class="nav-item pull-right">
                        {% if app.user %}
                            <a class="nav-link" href="{{ path('app_logout') }}">Logout</a>
                        {% else %}
                            <a class="nav-link" href="{{ path('app_login') }}">Login</a>
                        {% endif %}
                    </li>
                </ul>
            </div>
        </nav>
    </header>
    {% block body %}
    {% endblock %}
    </body>
</html>
